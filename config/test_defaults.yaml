seed: 69
log: True
cpu: 1
gpu: 1
num_nodes: 1

defaults:
  - _self_
  - model: gpt2
  - data_module: max
  - dataset: e2e_nlg
  - transform: no_transform
  - loss: mse
  - user_overrides: null

callbacks:

tasks:
  _target_: tasks.generate_text.TextGenerationModule
  

pretrained: # lightning_logs\car_version\checkpoints\ # add path to the pretrained weights